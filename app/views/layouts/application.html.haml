!!!
%html
  %head
    %meta(name='viewport' content='width=device-width, initial-scale=1.0')
    %title= content_for?(:title) ? "#{yield(:title)} | #{ENV['SITE_NAME']}" : ENV['SITE_NAME']

    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'application'

    = csrf_meta_tags

    %link(rel='stylesheet' href='//fonts.googleapis.com/css?family=Montserrat:400,700|Varela+Round')

  %body{class: (content_for?(:body_classes) ? yield(:body_classes) : 'weak')}
    %header
      = render 'layouts/navigation'

    = render 'layouts/messages'

    = yield

    .footer-and-credits
      %footer
        .container
          .row
            .col-xs-12.col-sm-4.pull-right
              = image_tag 'roccia.jpg', alt: 'Se non vieni, Roccia ti sgama.', class: 'roccia img-circle img-responsive'

            .col-xs-12.col-sm-8
              %h4 Serve una mano?

              %p
                Se hai bisogno di aiuto, contattaci all'indirizzo email
                %strong herudio@karmapen.com.
                Saremo felici di aiutarti con
                l'iscrizione, l'organizzazione dei corsi e l'uso della piattaforma
                online o parla con i rappresentanti d'istituto (Biondo, Roccetti,
                Caporali e Latini).

      .credits
        .container
          Fatto con
          = fa_icon 'heart', class: 'heart-icon'
          da
          = link_to('Desa', 'http://alessandro1997.github.io') + '.'
