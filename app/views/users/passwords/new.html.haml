- provide :title, t('devise.passwords.new.title')

.container-sm
  %h1= t('devise.passwords.new.title')

  .strong
    = simple_form_for(resource, url: user_password_path, html: { method: :post }) do |f|
      = f.input :email, input_html: { class: 'input-lg' }, required: true, autofocus: true

      .form-actions
        %button.btn.btn-lg.btn-primary(type='submit')
          = t('devise.passwords.new.submit')
